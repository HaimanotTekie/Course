// ---------------------------------------------------------------------
// Generated with Genova.
// ---------------------------------------------------------------------
package no.course.dialog;

import javax.swing.SwingUtilities;

/**
 * The customizable application class which starts the application. 
 * 
 * <p>To ensure that all exeptions and errors are caught, 
 * clients should add a default uncaught exception handler, see
 * {@link Thread#setDefaultUncaughtExceptionHandler(java.lang.Thread.UncaughtExceptionHandler)}.
 */
public class Student_appl extends Student_applDefault { 

    public static void main(String[] args) { 
        SwingUtilities.invokeLater(new Runnable() {
            public void run() {
                // Registry registry = Registry.getRegistry();
                Student_appl rs = null;
                rs = new Student_appl(); 
                rs.setApplication(); 
                rs.init();
                rs.start();
                rs.applicationFrame.setTitle("Student_appl");
            }
        });
    } 
} 
